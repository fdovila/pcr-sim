# encoding: utf-8

''' 

I decided to drop BLAST in favor of writing my own search algorithm.

It's faster because there's no disk IO

'''

class Search:
    ''' Representing a \"fuzzy\" search '''    
    @classmethod
    def find(self, template, query):
        ''' The search algorithm.  Returns start '''
        pass
        